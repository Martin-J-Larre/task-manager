extends layout

include ./mixins/createProject

block content
    .new-project-container
        h1 #{ pageName }

        pre=vardump(project)

        if errors 
            each error in errors
                .alert= error.text
        +createProject(project)

